// A implementação vive no AuthProvider, que mantém uma única subscription de
// auth para toda a aplicação. Este arquivo permanece como ponto de importação.
export { useAuth } from '@/contexts/AuthContext';
